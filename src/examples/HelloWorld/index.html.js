const html = `<p><code>pragma</code> specifies the compiler version of Solidity.</p>
<pre><code class="language-solidity">// compiler version must be greater than 0.5.0 and less than 0.6.0
pragma solidity ^0.5.1;

contract HelloWorld {
  string public greet = "Hello World!";
}
</code></pre>
`

export default html
